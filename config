# ncnyaa config file
#
# You can move this file to $XDG_CONFIG_HOME/ncnyaa/config
# or $HOME/.config/ncnyaa/config

# Download torrent file when selected (yes/no)
download=yes

# Torrent download path
path=/media/veracrypt2/TRn/torrentfiles/

# Run following command after selecting a torrent
#   $TITLE = torrent title
#   $URL = remote url
#   $FILE = local torrent (if any)
#cmd=xdg-open $FILE
cmd=qbittorrent "$FILE"
copycmd=echo "$URL" | xclip -selection clipboard

# Default category number
category=3
